# Mon Contrat (Hardhat)

## Setup

1. Copier `.env.example` en `.env`
2. Remplir `PRIVATE_KEY` et `ALCHEMY_URL`
3. Compiler:

npm run compile

## Deploiement

Ganache:

npm run deploy:ganache

Sepolia:

npm run deploy:sepolia

## ABI

npm run abi:extract
